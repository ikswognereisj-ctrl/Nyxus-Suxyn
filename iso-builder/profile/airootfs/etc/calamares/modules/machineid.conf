---
systemd: true
dbus: true
# `symlink:` is the deprecated spelling — the shipped 3.3.15 logs
# "configuration setting *symlink* is deprecated, use *dbus-symlink*" on every
# start (seen in the 2026-08-13 session log). Same behavior, current name.
dbus-symlink: true
