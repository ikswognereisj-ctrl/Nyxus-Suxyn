# NYXUS ISO default — generic, any panel.
# Settings ▸ Display rewrites this on the installed system.
#
# HARD RULE: the live stick must not ship the baker's laptop line
#   monitor = eDP-1, 1920x1200@165, 0x0, 1
# That 165 Hz pin is local to one Alienware. Copilot's fallback is
# correct FOR THE STICK, wrong to apply blindly on that laptop.
monitor = , preferred, auto, 1
